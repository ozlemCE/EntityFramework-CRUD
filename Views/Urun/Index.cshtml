@model IEnumerable<EntityFrameworkIslemler.Models.Urun>

@{
    ViewBag.Title = "Index";
}

<h2>Tüm Ürünler</h2>

<p>
 @Html.ActionLink("Ürün Ekle", "YeniUrun") 
 @Html.ActionLink("Kategoriler", "Index", "Kategori") 
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kategori.Adi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fiyat)
        </th>
        <th>

        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stok)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kategori.Adi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fiyat)
        </td>
        <td>

        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stok)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Duzenle", new { id=item.Id }) |
            @Html.ActionLink("Detay", "Detay", new { id=item.Id }) |
            @Html.ActionLink("Sil", "UrunSil", new { id=item.Id })
        </td>
    </tr>
}

</table>
